# Pinned-loosely stack for the TKG solar-forecasting reproduction.
# Preferred install path uses uv + pyproject.toml (CPU torch index configured there):
#   uv sync                      # core deps, CPU torch
#   uv sync --extra dev          # + pytest
#   uv sync --extra data         # + satpy/requests for live Himawari/NSRDB pulls
#
# Plain-pip fallback (CPU torch):
#   pip install --index-url https://download.pytorch.org/whl/cpu torch torchvision
#   pip install -r requirements.txt
#
# torch_geometric extras (pyg-lib, torch-scatter, torch-sparse) are NOT required for the
# dense GATConv path used here. Install matched wheels only if scaling to large sparse graphs:
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html

torch>=2.2
torchvision>=0.17
torch_geometric>=2.5
timm>=0.9
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
h5py>=3.8
matplotlib>=3.7
tqdm>=4.65
pyyaml>=6.0

# Comparison baselines (optional): ARIMA + TFT.
#   pip install statsmodels pytorch-forecasting
# statsmodels powers the ARIMA eval-only baseline; pytorch-forecasting is the
# TFT follow-up (heavy, version-sensitive — verify on Colab).
